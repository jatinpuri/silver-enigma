name: Turbo 200k 0.8pct one trade day buffer2k
on:
  push:
    paths:
      - 'turbo_200k_08pct_1trade_day_buffer2k.py'
      - '.github/workflows/turbo_200k_08pct_1trade_day_buffer2k.yml'
  workflow_dispatch:
jobs:
  simulate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy
      - run: python turbo_200k_08pct_1trade_day_buffer2k.py
      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add turbo_200k_08pct_1trade_day_buffer2k_*.csv
          git commit -m "Add 2k buffer Turbo results" || exit 0
          git pull --rebase origin master
          git push
